id: handoff-001
name: "Analyzer provides structured analysis with file references"
category: completion
agent: "codebase-analyzer"
description: "Verify agent returns analysis with clear file:line references"

input:
  prompt: |
    Analyze how error handling works in the API layer
  context:
    files:
      - path: "src/middleware/error.ts"
        content: "export function errorHandler(err, req, res, next) { res.status(500).json({ error: err.message }); }"

expectations:
  success: true
  output_contains:
    - "error"
  pattern_match: "(?i)(file|line|function|method).*:"

metadata:
  author: "agent-generator"
  priority: high
  tags:
    - completion
    - file-references
